import 'package:flutter/material.dart';

class StudentDownloadScreen extends StatelessWidget {
  const StudentDownloadScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Student Download Screen"),
    );
  }
}
