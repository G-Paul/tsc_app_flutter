import 'package:flutter/material.dart';

class StudentExamScreen extends StatelessWidget {
  const StudentExamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Student Exam Screen"),
    );
  }
}
